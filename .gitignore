node_modules
build
npm-debug.log
.env
.DS_Store
.env.test
package-lock.json
.vscode
.claude
.mcp.json
*_internal.md
deploy-commands-internal.bat
.playwright-mcp

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
tests/

# GitHub Skill 參考庫（學習用，不納入版控）
Skills/references/
.mcp_db_config.json
.mcp_sftp_presets.json

# Memory（客戶/個人知識：業務流程、PHP 檔名、專案名稱，只留本機，不進版控）
memory/

# 客戶專案專屬 Skill（含業務流程/架構/PHP 檔名，只留本機，不進版控）
Skills/commands/_cold/
Skills/commands/testing/_project_qc/

# Playwright 截圖（本機測試用，不進版控）
/screenshot/

reports/

# Flyway 實際連線設定（含帳密，不進版控）
# *.sample.toml 和 template_*.toml 仍會進版控
flyway/conf/*.toml
!flyway/conf/*.sample.toml
!flyway/conf/template_*.toml
config.local.js

# Improvements backlog（含專案名稱，不進版控）
improvements_backlog.md

# 對話 / session 暫存（retro/audit 中間檔，不進版控）
tmp/

# GSheet 對帳暫存（含憑證 .gsheet_creds.json + 一次性腳本/資料，不進版控）
_gsheet_audit/

# Windows reserved device names (accidentally created by > nul instead of > /dev/null)
nul
credentials.json
